{
	"name": "allenheath-dlive-ilive",
	"version": "2.0.0",
	"main": "index.js",
	"scripts": {
		"lint:raw": "eslint --ext .ts --ext .js --ignore-pattern dist --ignore-pattern pkg",
		"lint": "yarn lint:raw .",
		"format": "prettier -w .",
		"dev": "tsc -p tsconfig.json --watch",
		"build": "rimraf dist && yarn build:main",
		"build:main": "tsc -p tsconfig.json"
	},
	"license": "MIT",
	"repository": {
		"type": "git",
		"url": "git+https://github.com/bitfocus/companion-module-allenheath-dlive-ilive.git"
	},
	"dependencies": {
		"@companion-module/base": "~1.5"
	},
	"devDependencies": {
		"@companion-module/tools": "1.4.1",
		"eslint": "^8.51.0",
		"rimraf": "^5.0.5",
		"typescript": "^5.2.2"
	},
	"prettier": "@companion-module/tools/.prettierrc.json"
}
